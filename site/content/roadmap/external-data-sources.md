---
title: External Data Sources
description: Fetch JSON/YAML from URLs at build time and inject into template context
tags:
- planned
weight: 3
---

Fetch JSON/YAML from URLs at build time and inject into template context. A `[data_sources]` section in `seite.toml` with `name`, `url`, and `format` fields. Fetch at build step 3, merge with filesystem content. Enables pulling content from any CMS, API, or database.
